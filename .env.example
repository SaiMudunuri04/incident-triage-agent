# Placeholders only; use workload identity and secret management.
RUNBOOK_ROOT=/data/runbooks
METRICS_PATH=/data/metrics.json
LLM_BASE_URL=https://<your-inference-host>/v1
LLM_MODEL=<model-id>
LLM_API_KEY=<set-in-secret-manager>
